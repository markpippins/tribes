export interface PatternStatus {
    pattern: number
    position: number
    activeStep: number
    length: number
    firstStep: number
    lastStep: number
    direction: number
    muted: boolean
}
